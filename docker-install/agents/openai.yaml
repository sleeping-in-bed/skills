interface:
  display_name: "Docker Install"
  short_description: "Install Docker Engine from docker/docker-install"
  default_prompt: "Use $docker-install to install or verify Docker Engine using docker/docker-install."
