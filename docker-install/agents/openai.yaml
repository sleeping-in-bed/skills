interface:
  display_name: "Docker Install"
  short_description: "Install Docker Engine and NVIDIA GPU container support"
  default_prompt: "Use $docker-install to install Docker Engine and configure NVIDIA GPU containers when available."
